import { Link } from "react-router-dom";

const Login = () => {
  return (
    <Link to="/login">
      <button variant="outlined">
        Log In
      </button>
    </Link>
  );
}
  
export default Login;

