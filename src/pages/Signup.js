import { Link } from "react-router-dom";

const Signup = () => {
  return (
    <Link to="/signup">
      <button variant="outlined">
        Sign up
      </button>
    </Link>
  );
}
  
export default Signup;

